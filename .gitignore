/target
frontend/node_modules
frontend/dist

# Dev-only game-state dump (src/server.rs) — not real persistence, safe to
# delete any time you want a fresh start.
/dev_state.json

# Entirely generated by `cd frontend && npm run build` (vite.config.ts
# outputs here) — regenerate after cloning, nothing here is hand-edited.
/static
